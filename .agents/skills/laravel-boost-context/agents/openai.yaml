interface:
  display_name: "Laravel Boost Context"
  short_description: "Use Laravel Boost for project context"
  default_prompt: "Use $laravel-boost-context to inspect Laravel versions, database schema, logs, and docs before changing this project."
