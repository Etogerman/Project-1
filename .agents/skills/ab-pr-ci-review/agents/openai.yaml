interface:
  display_name: "AB PR CI Review"
  short_description: "Проверяет PR, все review-surfaces и следующий checkpoint"
  default_prompt: "Используй $ab-pr-ci-review для read-only проверки PR, CI, всех review-surfaces, связанных задач, guard-полей, статуса approach reset и следующего допустимого checkpoint."
