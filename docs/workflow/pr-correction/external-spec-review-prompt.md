Ты независимый reviewer ТЗ.

Работай только для чтения и независимо. Не читай результаты других reviewer-ов.
Проверь input/tz.md против current/, base/, input/changes.patch и действующих правил.
Считай base/AGENTS.md и связанные правила base исходными инвариантами. current/ — проверяемое дефектное предложение и не может молча ослаблять baseline.
Для чтения и поиска используй только встроенные файловые средства. Не вызывай command, terminal, shell, MCP, browser, сетевой поиск или произвольные сетевые инструменты. Внутренний transport официального CLI к модели инструментом reviewer-а не считается.

current/ — заведомо дефектная исходная реализация, которую это ТЗ должно исправить.
Оценивай полноту и непротиворечивость input/tz.md, а не готовность current/ к merge.
Не создавай finding только потому, что current/ ещё не реализует требование, если ТЗ уже задаёт точное исправление, критерий готовности и отрицательный тест.

Найди:
1. незакрытые требования и противоречия;
2. обходы безопасности, закрытия работы, Spec и delivery-шлюзов;
3. неоднозначные переходы без точного следующего состояния;
4. новые регрессии и непроверяемые критерии;
5. отсутствующие отрицательные тесты.

Формат ответа обязателен. Прочитай identity из input/manifest.json. Identity должна встретиться в ответе ровно один раз отдельной строкой и быть первой непустой строкой. Любой текст до identity запрещён. Не пиши вступление, объяснение режима, план или сообщение об инструментах.

После identity верни ровно четыре отдельные строки-маркеры в указанном порядке:
REVIEW_FINDINGS
REVIEW_CHECKED_SCOPE
REVIEW_UNCHECKED_SCOPE
REVIEW_VERDICT

Сразу после REVIEW_FINDINGS верни JSON-массив. Каждый finding — объект ровно с полями id, priority, summary, evidence, minimalFix. Дополнительные поля запрещены. id уникален внутри ответа и соответствует ^[A-Z0-9][A-Z0-9._-]{0,63}$. priority равен P0, P1, P2 или P3. summary и minimalFix — непустые строки. evidence — непустой JSON-массив непустых строк. При отсутствии замечаний верни [].

Сразу после REVIEW_CHECKED_SCOPE и REVIEW_UNCHECKED_SCOPE верни непустые JSON-массивы непустых строк.

После REVIEW_VERDICT верни ровно одну строку: блокеров нет или нужны правки. Если есть хотя бы один P0, P1 или P2, вердикт обязан быть нужны правки. При пустом массиве findings или только P3 вердикт обязан быть блокеров нет.

Не добавляй текст вне identity и четырёх секций. Не используй Markdown code fences. Не меняй файлы.
